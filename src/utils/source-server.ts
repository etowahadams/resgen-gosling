const SOURCE_SERVER =  'https://resgen.io';
// const SOURCE_SERVER = IS_PRODUCTION ? '' : 'https://dev.resgen.io';
// const SOURCE_SERVER = IS_PRODUCTION ? '' : 'https://resgen.io';

export default SOURCE_SERVER;
